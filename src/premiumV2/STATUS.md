Current state: premium report v2 is integrated behind hidden API routes while the existing live customer paid-report CTA remains unchanged. Verified in Vercel build: 14-page structured fixture rendering, A4 bounds, footer safe zone, minimum long-body font size, approved Divya logo rendering, high-resolution PNG previews, complete 14-page final PDF composition, OpenAI API key presence, configured paid model call, and strict structured JSON output. Remaining production blocker: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY or SUPABASE_SECRET_KEY are not configured in Vercel. After those two values are added, run one complete isolated v2 job and verify automated visual QA, private storage, signed PDF download, and final benchmark comparison before switching the live CTA.